using System.Linq;
using CustomLogic.Core.Interfaces;
using CustomLogic.Core.Interfaces.DL;
using CustomLogic.Core.Models;

namespace CustomLogic.Services.UserProfilesService
{
public class ReturnResultEvent : IUpdateEvent<UserProfilesViewModel, UserProfile>
{
    public int priority()
    {
        return 1000;
    }

    public bool Run(UserProfilesViewModel model, ref IQueryable< UserProfile> repository, IUnitOfWork unitOfWork, Response<UserProfilesViewModel>result, ICoreUser user)
    {
        var updatedDbModel = unitOfWork.With<UserProfile>().Single(c => c.Id == model.Id); // Might be a partial primary key template needs 
        var newCustomResult = UserProfiles
        Mapper.MapDbModelToViewModel(updatedDbModel);
        result.Data = newCustomResult;
        return true;
    }
}

