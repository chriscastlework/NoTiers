using System.Linq;
using CustomLogic.Core.Interfaces;
using CustomLogic.Core.Interfaces.DL;
using CustomLogic.Core.Models;

namespace CustomLogic.Services.ClientCorsOriginsService
{
public class ReturnResultEvent : IUpdateEvent<ClientCorsOriginsViewModel, ClientCorsOrigin>
{
    public int priority()
    {
        return 1000;
    }

    public bool Run(ClientCorsOriginsViewModel model, ref IQueryable< ClientCorsOrigin> repository, IUnitOfWork unitOfWork, Response<ClientCorsOriginsViewModel>result, ICoreUser user)
    {
        var updatedDbModel = unitOfWork.With<ClientCorsOrigin>().Single(c => c.Id == model.Id); // Might be a partial primary key template needs 
        var newCustomResult = ClientCorsOrigins
        Mapper.MapDbModelToViewModel(updatedDbModel);
        result.Data = newCustomResult;
        return true;
    }
}

