using System.Linq;
using CustomLogic.Core.Interfaces;
using CustomLogic.Core.Interfaces.DL;
using CustomLogic.Core.Models;

namespace CustomLogic.Services.UserFoldersService
{
public class ReturnResultEvent : IUpdateEvent<UserFoldersViewModel, UserFolder>
{
    public int priority()
    {
        return 1000;
    }

    public bool Run(UserFoldersViewModel model, ref IQueryable< UserFolder> repository, IUnitOfWork unitOfWork, Response<UserFoldersViewModel>result, ICoreUser user)
    {
        var updatedDbModel = unitOfWork.With<UserFolder>().Single(c => c.Id == model.Id); // Might be a partial primary key template needs 
        var newCustomResult = UserFolders
        Mapper.MapDbModelToViewModel(updatedDbModel);
        result.Data = newCustomResult;
        return true;
    }
}

