using System.Linq;
using CustomLogic.Core.Interfaces;
using CustomLogic.Core.Interfaces.DL;
using CustomLogic.Core.Models;

namespace CustomLogic.Services.CustomObjectModifyFieldsService
{
public class ReturnResultEvent : IUpdateEvent<CustomObjectModifyFieldsViewModel, CustomObjectModifyField>
{
    public int priority()
    {
        return 1000;
    }

    public bool Run(CustomObjectModifyFieldsViewModel model, ref IQueryable< CustomObjectModifyField> repository, IUnitOfWork unitOfWork, Response<CustomObjectModifyFieldsViewModel>result, ICoreUser user)
    {
        var updatedDbModel = unitOfWork.With<CustomObjectModifyField>().Single(c => c.Id == model.Id); // Might be a partial primary key template needs 
        var newCustomResult = CustomObjectModifyFields
        Mapper.MapDbModelToViewModel(updatedDbModel);
        result.Data = newCustomResult;
        return true;
    }
}

