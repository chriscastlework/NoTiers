using System;
using System.Linq;
using System.Net;
using System.Web.Mvc;
using CustomLogic;
using CustomLogic.Core.Interfaces;
using CustomLogic.Core.Models;
using CustomLogic.Services.OAuth2TokenService;


public class OAuth2TokenController : Controller
{
    //private AomDbContext db = new AomDbContext();
    private readonly IService<OAuth2TokenViewModel>_OAuth2TokenService = new OAuth2TokenService(new DbContext());


        // GET: OAuth2Token
        public ActionResult Index()
        {
        return View(_OAuth2Token
        Service.List(new NgTableParams() {page = 1, count = 1000}, null).Data.ToList());
        }

        // GET: OAuth2Token/Details/5
        public ActionResult Details(Guid? id)
        {
        if (id == null)
        {
        return new HttpStatusCodeResult(HttpStatusCode.BadRequest);
        }
        return View(_OAuth2Token
        Service.View(new OAuth2Token
        ViewModel() { ID = id.Value}, null));
        }

        // GET: OAuth2Token/Create
        public ActionResult Create()
        {
        return View();
        }

        // POST: OAuth2Token/Create
        // To protect from overposting attacks, please enable the specific properties you want to bind to, for
        // more details see http://go.microsoft.com/fwlink/?LinkId=317598.
        [HttpPost]
        [ValidateAntiForgeryToken]
        public ActionResult Create([Bind(Include = "Id,Name")] OAuth2Token
        ViewModel aomMeta)
        {
        if (ModelState.IsValid)
        {
        var result =_OAuth2Token
        Service.Insert(aomMeta, null);
        if(result.Success)
        return RedirectToAction("Index");

        // what if it went wrong?
        }

        return View(aomMeta);
        }

        // GET: OAuth2Token
        s/Edit/5
        public ActionResult Edit(Guid? id)
        {
        if (id == null)
        {
        return new HttpStatusCodeResult(HttpStatusCode.BadRequest);
        }
        var result = _OAuth2Token
        Service.View(new OAuth2Token
        ViewModel {ID = id.Value}, null);
        if (!result.Success)
        {
        return HttpNotFound();
        }
        return View(result.Data);
        }

        // POST: OAuth2Token
        s/Edit/5
        // To protect from overposting attacks, please enable the specific properties you want to bind to, for
        // more details see http://go.microsoft.com/fwlink/?LinkId=317598.
        [HttpPost]
        [ValidateAntiForgeryToken]
        public ActionResult Edit([Bind(Include = "Id,Name")] OAuth2Token
        ViewModel aomMeta)
        {
        if (ModelState.IsValid)
        {
        var result = _OAuth2Token
        Service.Update(aomMeta, null);
        if(result.Success)
        return RedirectToAction("Index");

        // what if it goes wrong?
        }
        return View(aomMeta);
        }

        // GET: OAuth2Token
        s/Delete/5
        public ActionResult Delete(Guid? id)
        {
        if (id == null)
        {
        return new HttpStatusCodeResult(HttpStatusCode.BadRequest);
        }
        var result = _OAuth2Token
        Service.View(new OAuth2Token
        ViewModel {ID = id.Value}, null);
        if (!result.Success)
        {
        return HttpNotFound();
        }
        return View(result.Data);
        }

        // POST: OAuth2Token
        s/Delete/5
        [HttpPost, ActionName("Delete")]
        [ValidateAntiForgeryToken]
        public ActionResult DeleteConfirmed(Guid id)
        {
        var result = _OAuth2Token
        Service.Delete(new OAuth2Token
        ViewModel {ID = id}, null);
        return RedirectToAction("Index");
        }

        protected override void Dispose(bool disposing)
        {
        if (disposing)
        {
        // _OAuth2Token
        Service = null; // make services disposeable?
        }
        base.Dispose(disposing);
        }
        }


