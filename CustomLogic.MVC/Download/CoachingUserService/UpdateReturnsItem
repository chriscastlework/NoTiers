using System.Linq;
using CustomLogic.Core.Interfaces;
using CustomLogic.Core.Interfaces.DL;
using CustomLogic.Core.Models;

namespace CustomLogic.Services.CoachingUserService
{
public class ReturnResultEvent : IUpdateEvent<CoachingUserViewModel, CoachingUser>
{
    public int priority()
    {
        return 1000;
    }

    public bool Run(CoachingUserViewModel model, ref IQueryable< CoachingUser> repository, IUnitOfWork unitOfWork, Response<CoachingUserViewModel>result, ICoreUser user)
    {
        var updatedDbModel = unitOfWork.With<CoachingUser>().Single(c => c.Id == model.Id); // Might be a partial primary key template needs 
        var newCustomResult = CoachingUser
        Mapper.MapDbModelToViewModel(updatedDbModel);
        result.Data = newCustomResult;
        return true;
    }
}

